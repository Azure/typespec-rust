// Copyright (c) Microsoft Corporation. All rights reserved.
//
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::models::{
    RoutesPathParametersLabelExpansionStandardClientArrayOptions,
    RoutesPathParametersLabelExpansionStandardClientPrimitiveOptions,
    RoutesPathParametersLabelExpansionStandardClientRecordOptions,
};
use azure_core::{
    error::{ErrorKind, HttpError},
    http::{Context, Method, NoFormat, Pipeline, Request, Response, Url},
    tracing, Error, Result,
};
use std::collections::HashMap;

#[tracing::client]
pub struct RoutesPathParametersLabelExpansionStandardClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

impl RoutesPathParametersLabelExpansionStandardClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Routes.PathParameters.LabelExpansion.Standard.array")]
    pub async fn array(
        &self,
        param: &[&str],
        options: Option<RoutesPathParametersLabelExpansionStandardClientArrayOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = Context::with_context(&options.method_options.context);
        let mut url = self.endpoint.clone();
        let mut path = String::from("routes/path/label/standard/array{param}");
        path = path.replace("{param}", &format!(".{}", param.join(",")));
        url = url.join(&path)?;
        let mut request = Request::new(url, Method::Get);
        let rsp = self.pipeline.send(&ctx, &mut request).await?;
        if !rsp.status().is_success() {
            let status = rsp.status();
            let http_error = HttpError::new(rsp).await;
            let error_kind = ErrorKind::http_response(
                status,
                http_error.error_code().map(std::borrow::ToOwned::to_owned),
            );
            return Err(Error::new(error_kind, http_error));
        }
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Routes.PathParameters.LabelExpansion.Standard.primitive")]
    pub async fn primitive(
        &self,
        param: &str,
        options: Option<RoutesPathParametersLabelExpansionStandardClientPrimitiveOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        if param.is_empty() {
            return Err(azure_core::Error::message(
                azure_core::error::ErrorKind::Other,
                "parameter param cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = Context::with_context(&options.method_options.context);
        let mut url = self.endpoint.clone();
        let mut path = String::from("routes/path/label/standard/primitive{param}");
        path = path.replace("{param}", &format!(".{param}"));
        url = url.join(&path)?;
        let mut request = Request::new(url, Method::Get);
        let rsp = self.pipeline.send(&ctx, &mut request).await?;
        if !rsp.status().is_success() {
            let status = rsp.status();
            let http_error = HttpError::new(rsp).await;
            let error_kind = ErrorKind::http_response(
                status,
                http_error.error_code().map(std::borrow::ToOwned::to_owned),
            );
            return Err(Error::new(error_kind, http_error));
        }
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Routes.PathParameters.LabelExpansion.Standard.record")]
    pub async fn record(
        &self,
        param: HashMap<String, i32>,
        options: Option<RoutesPathParametersLabelExpansionStandardClientRecordOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = Context::with_context(&options.method_options.context);
        let mut url = self.endpoint.clone();
        let mut path = String::from("routes/path/label/standard/record{param}");
        {
            let mut param_vec = param.iter().collect::<Vec<_>>();
            param_vec.sort_by_key(|p| p.0);
            path = path.replace(
                "{param}",
                &format!(
                    ".{}",
                    param_vec
                        .iter()
                        .map(|(k, v)| format!("{k},{v}"))
                        .collect::<Vec<_>>()
                        .join(",")
                ),
            );
        }
        url = url.join(&path)?;
        let mut request = Request::new(url, Method::Get);
        let rsp = self.pipeline.send(&ctx, &mut request).await?;
        if !rsp.status().is_success() {
            let status = rsp.status();
            let http_error = HttpError::new(rsp).await;
            let error_kind = ErrorKind::http_response(
                status,
                http_error.error_code().map(std::borrow::ToOwned::to_owned),
            );
            return Err(Error::new(error_kind, http_error));
        }
        Ok(rsp.into())
    }
}
