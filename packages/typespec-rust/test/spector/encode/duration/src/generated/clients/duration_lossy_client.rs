// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::models::{
    DurationLossyClientIntMillisecondsOptions, DurationLossyClientIntSecondsOptions,
};
use azure_core::{
    error::CheckSuccessOptions,
    http::{Method, NoFormat, Pipeline, PipelineSendOptions, Request, Response, Url, UrlExt},
    tracing, Result,
};

/// Lossy encode scenarios.
///
/// These scenarios cover the case where the source `duration` carries more precision than the
/// target encoding can represent (e.g. a sub-second value encoded as integer seconds). The client
/// must still serialize the value using the target number type (an integer), discarding the extra
/// precision, rather than emitting a floating point number. This is distinct from arbitrary type
/// mismatches, which are already covered by the round-trip scenarios above.
#[tracing::client]
pub struct DurationLossyClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

impl DurationLossyClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Encode.Duration.Lossy.intMilliseconds")]
    pub async fn int_milliseconds(
        &self,
        input: i32,
        options: Option<DurationLossyClientIntMillisecondsOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/encode/duration/lossy/int32-milliseconds");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("input", input.to_string());
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Encode.Duration.Lossy.intSeconds")]
    pub async fn int_seconds(
        &self,
        input: i32,
        options: Option<DurationLossyClientIntSecondsOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/encode/duration/lossy/int32-seconds");
        let mut query_builder = url.query_builder();
        query_builder.set_pair("input", input.to_string());
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}
