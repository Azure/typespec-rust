// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::models::{
    EnumPathParamsClientNumericOptions, EnumPathParamsClientOptionalExtensibleOptions,
    EnumPathParamsClientOptionalFixedOptions, ExtensibleShape, FixedShape, NumericValues,
};
use azure_core::{
    error::CheckSuccessOptions,
    fmt::SafeDebug,
    http::{
        ClientOptions, Method, NoFormat, Pipeline, PipelineSendOptions, Request, Response, Url,
        UrlExt,
    },
    tracing, Result,
};

#[tracing::client]
pub struct EnumPathParamsClient {
    pub(crate) bogus_url: Url,
    pub(crate) pipeline: Pipeline,
}

/// Options used when creating a [`EnumPathParamsClient`](EnumPathParamsClient)
#[derive(Clone, Default, SafeDebug)]
pub struct EnumPathParamsClientOptions {
    /// Allows customization of the client.
    pub client_options: ClientOptions,
}

impl EnumPathParamsClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.bogus_url
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("EnumPathParams.numeric")]
    pub async fn numeric(
        &self,
        value: NumericValues,
        options: Option<EnumPathParamsClientNumericOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.bogus_url.clone();
        let mut path = String::from("/numeric/{value}");
        path = path.replace("{value}", &value.to_string());
        url.append_path(&path);
        let mut request = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("EnumPathParams.optionalExtensible")]
    pub async fn optional_extensible(
        &self,
        shape: ExtensibleShape,
        options: Option<EnumPathParamsClientOptionalExtensibleOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        if shape.as_ref().is_empty() {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                "parameter shape cannot be empty",
            ));
        }
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.bogus_url.clone();
        let mut path = String::from("/optional/{shape}/{value}");
        path = path.replace("{shape}", shape.as_ref());
        path = match options.value {
            Some(value) => path.replace("{value}", value.as_ref()),
            None => path.replace("{value}", ""),
        };
        url.append_path(&path);
        let mut request = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("EnumPathParams.optionalFixed")]
    pub async fn optional_fixed(
        &self,
        shape: FixedShape,
        options: Option<EnumPathParamsClientOptionalFixedOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.bogus_url.clone();
        let mut path = String::from("/fixed/{shape}/{value}");
        path = path.replace("{shape}", shape.as_ref());
        path = match options.value {
            Some(value) => path.replace("{value}", value.as_ref()),
            None => path.replace("{value}", ""),
        };
        url.append_path(&path);
        let mut request = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}
